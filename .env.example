# Required: PostgreSQL connection string
DATABASE_URL=

# Required for TMDB ingestion (server-side only; never NEXT_PUBLIC_)
# Bearer Read Access Token from TMDB account settings -> API
TMDB_API_TOKEN=
